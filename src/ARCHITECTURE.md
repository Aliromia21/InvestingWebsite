# 🏗️ Investment Platform - Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                    http://localhost:5173                         │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ HTTP/HTTPS Requests
             │ JSON Data
             │ JWT Tokens
             │
┌────────────▼────────────────────────────────────────────────────┐
│                    REACT FRONTEND                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Components Layer                                         │  │
│  │  • LandingPage    • Dashboard      • AdminDashboard      │  │
│  │  • LoginPage      • InvestmentPacks • UsersManagement    │  │
│  │  • SignupPage     • Transactions    • DepositRequests    │  │
│  │  • Referrals      • KYC Verification • Messages          │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Hooks Layer                                              │  │
│  │  • useAuth()  - Authentication state                     │  │
│  │  • useApi()   - API call wrapper                         │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Services Layer (9 Services)                             │  │
│  │  • api.service.ts        - HTTP client + JWT refresh     │  │
│  │  • auth.service.ts       - Login, signup, profile        │  │
│  │  • investment.service.ts - Investments                   │  │
│  │  • transaction.service.ts - Deposits, withdrawals        │  │
│  │  • referral.service.ts   - Referrals, commissions        │  │
│  │  • kyc.service.ts        - KYC verification              │  │
│  │  • message.service.ts    - Messaging                     │  │
│  │  • dashboard.service.ts  - Statistics                    │  │
│  │  • admin.service.ts      - Admin operations              │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Configuration                                            │  │
│  │  • api.config.ts - API endpoints & base URL              │  │
│  │  • types/api.ts  - TypeScript interfaces                 │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────┬────────────────────────────────────────────────────┘
             │
             │ REST API Calls
             │ Authorization: Bearer <JWT>
             │
┌────────────▼────────────────────────────────────────────────────┐
│                    DJANGO BACKEND                                │
│                 http://localhost:8000/api                        │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  URL Router (api/urls.py)                                │  │
│  │  • /auth/*        - Authentication endpoints             │  │
│  │  • /users/*       - User profile & stats                 │  │
│  │  • /investments/* - Investment operations                │  │
│  │  • /transactions/* - Deposit/withdrawal                  │  │
│  │  • /referrals/*   - Referral system                      │  │
│  │  • /kyc/*         - KYC verification                     │  │
│  │  • /messages/*    - Messaging system                     │  │
│  │  • /admin/*       - Admin operations                     │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Middleware                                               │  │
│  │  • CORS Headers                                           │  │
│  │  • JWT Authentication                                     │  │
│  │  • CSRF Protection                                        │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Views Layer (api/views.py)                              │  │
│  │  • signup_view()              - User registration        │  │
│  │  • login_view()               - JWT login                │  │
│  │  • user_stats_view()          - Dashboard stats          │  │
│  │  • create_investment_view()   - Investment creation      │  │
│  │  • deposit_view()             - Deposit request          │  │
│  │  • withdrawal_view()          - Withdrawal request       │  │
│  │  • referral_stats_view()      - Referral statistics      │  │
│  │  • submit_kyc_view()          - KYC submission           │  │
│  │  • admin_stats_view()         - Admin statistics         │  │
│  │  • approve_transaction_view() - Transaction approval     │  │
│  │  • ... (50+ total endpoints)                             │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Serializers (api/serializers.py)                        │  │
│  │  • UserSerializer                                         │  │
│  │  • SignupSerializer                                       │  │
│  │  • InvestmentPackSerializer                              │  │
│  │  • UserInvestmentSerializer                              │  │
│  │  • TransactionSerializer                                 │  │
│  │  • ReferralSerializer                                    │  │
│  │  • KYCVerificationSerializer                             │  │
│  │  • MessageSerializer                                     │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Business Logic                                           │  │
│  │  • Balance calculation                                    │  │
│  │  • Referral commission (3%)                              │  │
│  │  • Investment return calculation                         │  │
│  │  • Transaction approval workflow                         │  │
│  │  • KYC verification workflow                             │  │
│  │  • Milestone reward checking                             │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
│  ┌──────────────▼───────────────────────────────────────────┐  │
│  │  Models (api/models.py)                                   │  │
│  │  • User               - Custom user with balance          │  │
│  │  • InvestmentPack     - Investment packages               │  │
│  │  • UserInvestment     - User's investments                │  │
│  │  • Transaction        - All money movements               │  │
│  │  • ReferralPack       - Referral milestones               │  │
│  │  • ReferralCommission - Commission tracking               │  │
│  │  • KYCVerification    - KYC documents                     │  │
│  │  • Message            - User-Admin messages               │  │
│  └──────────────┬───────────────────────────────────────────┘  │
│                 │                                                │
└─────────────────┼────────────────────────────────────────────────┘
                  │
                  │ Django ORM
                  │
┌─────────────────▼────────────────────────────────────────────────┐
│                      DATABASE                                     │
│                    SQLite (Dev)                                   │
│                 PostgreSQL (Production)                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Tables                                                   │   │
│  │  • api_user                                               │   │
│  │  • api_investmentpack                                     │   │
│  │  • api_userinvestment                                     │   │
│  │  • api_transaction                                        │   │
│  │  • api_referralpack                                       │   │
│  │  • api_referralcommission                                 │   │
│  │  • api_kycverification                                    │   │
│  │  • api_message                                            │   │
│  └───────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### 1. User Registration Flow

```
User Browser
     │
     │ 1. Fill signup form
     │    (email, password, referral_code)
     ▼
React Frontend
     │
     │ 2. authService.signup(data)
     │    → api.service.post('/auth/signup/')
     ▼
Django Backend
     │
     │ 3. signup_view()
     │    → SignupSerializer
     │    → Validate data
     │    → Check referral code
     ▼
Database
     │
     │ 4. Create User
     │    → Generate referral_code
     │    → Link to referrer (if code provided)
     │    → Save to api_user table
     ▼
Response
     │
     │ 5. Return success message
     │    ← JSON: {message: "Registration successful"}
     ▼
React Updates
     │
     └─→ Show success
         Redirect to login
```

### 2. Investment Creation Flow

```
User
  │
  │ 1. Select investment pack
  │    Enter amount: $10,000
  ▼
Frontend
  │
  │ 2. Check pack limits
  │    Validate amount
  │    investmentService.createInvestment({
  │      pack_id: 2,
  │      amount: 10000
  │    })
  ▼
Backend - create_investment_view()
  │
  │ 3. Validate pack exists & active
  │    Validate amount in range ($5k-$20k ✓)
  │    Check user balance ($15,000 ✓)
  ▼
  │ 4. Create UserInvestment
  │    • amount: $10,000
  │    • daily_return: $500 (5% of $10,000)
  │    • start_date: today
  │    • end_date: today + 60 days
  │    • status: active
  ▼
  │ 5. Deduct from user balance
  │    user.balance: $15,000 → $5,000
  ▼
  │ 6. Check referrer
  │    user.referred_by exists? YES
  ▼
  │ 7. Calculate commission
  │    commission = $10,000 × 3% = $300
  ▼
  │ 8. Add to referrer balance
  │    referrer.balance += $300
  ▼
  │ 9. Create ReferralCommission record
  │    • referrer: user's referrer
  │    • referred_user: user
  │    • amount: $300
  │    • investment: new investment
  ▼
  │ 10. Create Transaction record
  │     • user: referrer
  │     • type: referral_commission
  │     • amount: $300
  │     • status: completed
  ▼
Response
  │
  └─→ Return investment data
      Update frontend UI
      Show success message
```

### 3. Admin Deposit Approval Flow

```
Admin Dashboard
      │
      │ 1. View pending deposits
      │    See user's deposit request:
      │    • User: john@example.com
      │    • Amount: $5,000
      │    • Wallet: 0x123...
      │    • Hash: 0xabc...
      ▼
Admin Action
      │
      │ 2. Click "Approve"
      │    Enter note: "Verified on blockchain"
      │    adminService.approveTransaction({
      │      transaction_id: 123,
      │      admin_note: "Verified on blockchain"
      │    })
      ▼
Backend - approve_transaction_view()
      │
      │ 3. Get transaction (ID: 123)
      │    Verify transaction.type == 'deposit' ✓
      │    Verify transaction.status == 'pending' ✓
      ▼
      │ 4. Add amount to user balance
      │    user.balance: $1,000 → $6,000
      │    user.save()
      ▼
      │ 5. Update transaction
      │    transaction.status = 'approved'
      │    transaction.admin_note = "Verified on blockchain"
      │    transaction.save()
      ▼
Response
      │
      │ 6. Return updated transaction
      │    status: 'approved'
      ▼
Frontend Update
      │
      └─→ Refresh deposit list
          Show success notification
          User sees updated balance
```

### 4. Referral System Flow

```
User A (Referrer)
      │
      │ Has referral_code: "ABC12345"
      │ balance: $0
      ▼
User B (New User)
      │
      │ 1. Signs up with code "ABC12345"
      │    System links: User B → referred_by → User A
      │
      │ 2. Gets deposit approved: +$10,000
      │    balance: $10,000
      ▼
User B Creates Investment
      │
      │ 3. Invests $10,000 in Professional pack
      │
      ▼
Backend Auto-Processing
      │
      │ 4. Check referred_by: exists ✓
      │    Calculate: $10,000 × 3% = $300
      │
      │ 5. User A.balance += $300
      │    User A.balance: $0 → $300
      │
      │ 6. Create ReferralCommission
      │    • referrer: User A
      │    • referred_user: User B
      │    • amount: $300
      │
      │ 7. Create Transaction
      │    • user: User A
      │    • type: referral_commission
      │    • amount: $300
      │    • status: completed
      ▼
Results
      │
      ├─→ User A: +$300 commission
      │           (sees in dashboard)
      │
      └─→ User B: Investment created
                  (continues earning daily)
```

## Authentication Flow

```
┌──────────────────────────────────────────────────────────────┐
│  LOGIN PROCESS                                                │
└──────────────────────────────────────────────────────────────┘

1. User enters credentials
   ↓
2. Frontend: authService.login({email, password})
   ↓
3. Backend: login_view()
   • Authenticate user
   • Generate JWT tokens
   • Return: {access, refresh, user}
   ↓
4. Frontend: Store tokens in localStorage
   • access_token (expires in 1 hour)
   • refresh_token (expires in 7 days)
   ↓
5. All subsequent requests include:
   Authorization: Bearer <access_token>

┌──────────────────────────────────────────────────────────────┐
│  TOKEN REFRESH (Automatic)                                    │
└──────────────────────────────────────────────────────────────┘

1. API call receives 401 Unauthorized
   ↓
2. ApiService detects expired token
   ↓
3. Auto-call: POST /auth/token/refresh/
   • Send refresh_token
   ↓
4. Receive new access_token
   ↓
5. Update localStorage
   ↓
6. Retry original request
   ↓
7. Return data to component

If refresh fails:
   → Clear tokens
   → Redirect to login
```

## Security Layers

```
┌─────────────────────────────────────────────────────────┐
│  FRONTEND SECURITY                                       │
├─────────────────────────────────────────────────────────┤
│  • JWT stored in localStorage                           │
│  • Auto token refresh                                   │
│  • Route protection (requires auth)                     │
│  • Role-based component rendering                       │
│  • Input validation                                     │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  TRANSPORT SECURITY                                      │
├─────────────────────────────────────────────────────────┤
│  • HTTPS (production)                                   │
│  • JWT in Authorization header                          │
│  • CORS protection                                      │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  BACKEND SECURITY                                        │
├─────────────────────────────────────────────────────────┤
│  • JWT Authentication                                   │
│  • Permission classes (IsAuthenticated, IsAdmin)        │
│  • Password hashing (Django default)                    │
│  • CSRF protection                                      │
│  • Role-based access control                            │
│  • Input sanitization                                   │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│  DATABASE SECURITY                                       │
├─────────────────────────────────────────────────────────┤
│  • SQL injection protection (ORM)                       │
│  • Encrypted passwords                                  │
│  • Foreign key constraints                              │
│  • Transaction atomicity                                │
└─────────────────────────────────────────────────────────┘
```

## Technology Stack

```
┌─────────────────────────────────────────────────────────┐
│  FRONTEND                                                │
├─────────────────────────────────────────────────────────┤
│  • React 18                                             │
│  • TypeScript                                           │
│  • Vite (build tool)                                    │
│  • Tailwind CSS                                         │
│  • Shadcn/ui components                                 │
│  • Lucide React (icons)                                 │
│  • Recharts (charts)                                    │
│  • React Router (routing)                               │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  BACKEND                                                 │
├─────────────────────────────────────────────────────────┤
│  • Django 4.2+                                          │
│  • Django REST Framework                                │
│  • Simple JWT                                           │
│  • Django CORS Headers                                  │
│  • Pillow (image handling)                              │
│  • Python 3.8+                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  DATABASE                                                │
├─────────────────────────────────────────────────────────┤
│  • SQLite (development)                                 │
│  • PostgreSQL (production recommended)                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  DEPLOYMENT (Future)                                     │
├─────────────────────────────────────────────────────────┤
│  Frontend: Vercel, Netlify, AWS S3                      │
│  Backend: Heroku, DigitalOcean, AWS EC2                 │
│  Database: AWS RDS, DigitalOcean Managed Database       │
│  Files: AWS S3, DigitalOcean Spaces                     │
└─────────────────────────────────────────────────────────┘
```

## File Structure Overview

```
project/
│
├── Frontend (React)
│   ├── components/        # UI Components
│   ├── services/          # API Services
│   ├── hooks/             # Custom Hooks
│   ├── types/             # TypeScript Types
│   ├── config/            # Configuration
│   ├── contexts/          # React Contexts
│   ├── styles/            # Global Styles
│   └── App.tsx            # Main App Component
│
├── Backend (Django)
│   └── django_backend/
│       ├── api/           # Main API App
│       │   ├── models.py      # Database Models
│       │   ├── views.py       # API Endpoints
│       │   ├── serializers.py # Data Serializers
│       │   ├── urls.py        # URL Routing
│       │   └── admin.py       # Admin Config
│       │
│       ├── investment_backend/  # Project Settings
│       │   ├── settings.py      # Configuration
│       │   └── urls.py          # Root URLs
│       │
│       ├── manage.py            # Django CLI
│       ├── requirements.txt     # Dependencies
│       └── db.sqlite3          # Database File
│
└── Documentation
    ├── QUICK_START.md                    # Quick reference
    ├── STARTUP_GUIDE.md                  # Setup guide
    ├── DJANGO_INTEGRATION_COMPLETE.md    # Integration
    ├── FINAL_INTEGRATION_SUMMARY.md      # Summary
    └── ARCHITECTURE.md                   # This file
```

## Deployment Architecture (Production)

```
┌────────────────────────────────────────────────────────────┐
│                         USERS                               │
│                  (Web Browsers/Mobile)                      │
└────────────┬───────────────────────────────────────────────┘
             │
             │ HTTPS
             │
┌────────────▼───────────────────────────────────────────────┐
│                      CDN / Static Host                      │
│                   (Vercel / Netlify)                        │
│                                                              │
│                  React App (Built)                          │
│                  • index.html                               │
│                  • assets/*.js                              │
│                  • assets/*.css                             │
└────────────┬───────────────────────────────────────────────┘
             │
             │ API Calls (HTTPS)
             │
┌────────────▼───────────────────────────────────────────────┐
│                    Load Balancer                            │
│                      (Optional)                             │
└────────────┬───────────────────────────────────────────────┘
             │
┌────────────▼───────────────────────────────────────────────┐
│                   Django Backend                            │
│              (Heroku / DigitalOcean / AWS)                  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Gunicorn (WSGI Server)                              │  │
│  │    ↓                                                  │  │
│  │  Django Application                                   │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────┬───────────────────────────────────────────────┘
             │
        ┌────┴────┬──────────┬──────────┐
        │         │          │          │
        ▼         ▼          ▼          ▼
  ┌─────────┬────────┬──────────┬──────────────┐
  │PostgreSQL│ Redis  │   S3     │  Email       │
  │Database  │ Cache  │  Files   │  Service     │
  │          │        │ (KYC)    │  (SendGrid)  │
  └──────────┴────────┴──────────┴──────────────┘
```

---

**This architecture provides:**
- ✅ Scalability
- ✅ Security
- ✅ Maintainability
- ✅ Performance
- ✅ Separation of Concerns
